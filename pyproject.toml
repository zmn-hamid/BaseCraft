[project]
name = "basecraft"
version = "0.1.0"
description = "Project for those who need to make tools but don't know anything"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyrefly>=1.1.1",
    "ruff>=0.15.20",
]
